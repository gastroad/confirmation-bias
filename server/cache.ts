// 캐시 수명만 여기서 관리한다. 실제 캐싱은 **DTO 경계**(API 라우트·서버 컴포넌트)에서 건다.
//
// 쿼리 레벨(Prisma row)에 걸면 안 된다 — `unstable_cache`는 반환값을 JSON 직렬화하므로
// `Date`가 문자열로 돌아와 `row.publishedAt.getTime()` 같은 호출이 깨진다.
// DTO는 이미 ISO 문자열이라 직렬화가 안전하다.
//
// 페이지 단위 `revalidate`를 쓰지 않는 이유: 헤더가 세션(쿠키)을 읽는 순간 모든 페이지가
// 동적이 되어 무력화된다. Neon은 compute-hour 과금이라 왕복 횟수 자체를 줄여야 한다.

/**
 * **DTO 모양이 바뀌면 이 값을 올린다.**
 *
 * `unstable_cache` 키에는 인자만 들어가고 반환 타입은 들어가지 않는다. DTO에 필드를
 * 추가·삭제해도 키가 그대로면 배포 후에도 **옛 모양의 응답이 TTL이 끝날 때까지 그대로
 * 서빙된다**(목록 1시간·상세 6시간). 새 클라이언트가 없는 필드를 읽어 `undefined`가 되고,
 * 화면에는 `NaN` 같은 값이 뜬다. 실제로 2026-08-25에 `tilt`·`outletCount`를 추가하면서
 * 프로덕션에서 이 일이 났다.
 *
 * 키마다 인자가 달라 캐시 항목도 갈리므로(`?date=` 유무 등) 일부만 멀쩡해 보일 수 있다.
 * 배포 직후 한 경로만 확인하고 넘어가면 놓친다.
 */
export const DTO_VERSION = "v3";

export const CACHE_TTL = {
  /** 클러스터 상세. 한번 만들어진 날짜의 클러스터는 굳으므로 길게 잡아도 된다. */
  clusterDetail: 60 * 60 * 6,
  /** 목록·집계. 최신 날짜가 하루 1회 바뀐다. */
  clusterList: 60 * 60,
  /** 날짜 축. 하루 1회 늘어난다. */
  days: 60 * 60,
  /** 사이트맵. 크롤러가 자주 와도 DB를 치지 않게. */
  sitemap: 60 * 60 * 6,
  /** 언론사 집계. 전체 기사를 훑는 쿼리라 자주 돌릴 수 없다. 하루 1회 배치로만 변한다. */
  outlets: 60 * 60 * 6,
} as const;
